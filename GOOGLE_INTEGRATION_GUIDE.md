# คู่มือการเชื่อมต่อข้อมูลผู้ผ่านการอบรมบอนไซเข้ากับ Google Sheets และ Google Drive

ระบบนี้ช่วยให้คุณสามารถ:
1. **จัดเก็บรายละเอียดผู้ผ่านการอบรมแต่ละรุ่น** ลงใน Google Sheets (แท็บ `Trainees`)
2. **จัดเก็บไฟล์รูปถ่ายผู้ผ่านการอบรม** ไว้ใน Google Drive โดยอัตโนมัติ (ในโฟลเดอร์ชื่อ `Bonsai_Trainees_Photos`)
3. **ซิงค์สองทางอัตโนมัติ**: เมื่อแก้ไขข้อมูล, ลบคน, หรืออัปโหลดรูปภาพใหม่จากหน้าเว็บ ระบบจะอัปเดตไปยัง Google Sheets และ Google Drive ให้ทันที

---

## 📋 โครงสร้างข้อมูลใน Google Sheets (แท็บ `Trainees`)

ระบบจะสร้างแผ่นงานแท็บชื่อ **`Trainees`** ให้อัตโนมัติใน Spreadsheet เดียวกับระบบสมาชิก โดยมีคอลัมน์ดังนี้:

| คอลัมน์ | ชื่อฟิลด์ | คำอธิบาย | ตัวอย่างข้อมูล |
|:---:|:---|:---|:---|
| A | `id` | รหัสผู้เข้าอบรม | `BKK-01-A008` |
| B | `batch` | รุ่นที่อบรม | `1` |
| C | `name` | ชื่อ - นามสกุลจริง | `นายพัศณิชภัท ธนาทรัพย์` |
| D | `nickname` | ชื่อเล่น | `คุณพัศ` |
| E | `role` | ตำแหน่งในรุ่น | `ประธานรุ่นที่ 1` |
| F | `certNo` | เลขที่ใบรับรอง | `TBA-CERT-2026-0101` |
| G | `status` | สถานะการอบรม | `จบหลักสูตร` |
| H | `photoUrl` | ลิงก์รูปถ่ายใน Google Drive | `https://drive.google.com/file/d/1W2.../view` |
| I | `cropFocusX` | จุดโฟกัสครอบรูป X (%) | `23` |
| J | `cropFocusY` | จุดโฟกัสครอบรูป Y (%) | `13` |
| K | `cropScale` | อัตราการซูม (Scale) | `1.85` |
| L | `updatedAt` | วันที่อัปเดตล่าสุด | `25/9/2569 11:30:00` |

---

## 🚀 ขั้นตอนการติดตั้ง (ทำเพียงครั้งเดียว)

### ขั้นตอนที่ 1: อัปเดตโค้ดใน Google Apps Script
1. เปิด Google Spreadsheet ของคุณ
2. ไปที่เมนู **ส่วนขยาย (Extensions)** > **Apps Script**
3. คัดลอกโค้ดทั้งหมดจากไฟล์ [`Code.gs`](file:///d:/Project%202026/bonsai-member/Code.gs) ในโปรเจกต์นี้
4. นำไปวางแทนที่โค้ดเดิมทั้งหมดในแท็บ `Code.gs` ของ Google Apps Script
5. กดปุ่ม **บันทึก (Save 💾)**

### ขั้นตอนที่ 2: Deploy เวอร์ชันใหม่
1. ที่มุมขวาบนของหน้า Google Apps Script กดปุ่ม **การทำให้ใช้งานได้ (Deploy)** > **จัดการการทำให้ใช้งานได้ (Manage deployments)**
2. คลิกไอคอน **ดินสอ (แก้ไข / Edit)**
3. ในช่อง **เวอร์ชัน (Version)** ให้เลือก **เวอร์ชันใหม่ (New version)**
4. กดปุ่ม **นำไปใช้งาน (Deploy)**

---

## ☁️ วิธีการส่งรูปและข้อมูลทั้งหมดขึ้น Google Drive & Sheets

คุณสามารถเลือกทำได้ 2 วิธีที่สะดวกมาก:

### วิธีที่ 1: กดปุ่มจากหน้าเว็บโดยตรง (ง่ายที่สุด)
1. เปิดหน้าเว็บทำเนียบผู้อบรม `http://localhost:3000/trainee-chart.html`
2. ที่แถบด้านบนจะมีปุ่ม **`☁️ ซิงค์เข้า Drive & Sheets`**
3. คลิกปุ่มนี้ ระบบจะอ่านรูปภาพทั้งหมดในโฟลเดอร์ แปลงและอัปโหลดขึ้น Google Drive พร้อมสร้างแถวข้อมูลลง Google Sheets ให้ครบทุกคนโดยอัตโนมัติ

### วิธีที่ 2: รันผ่านคำสั่ง Terminal (สำหรับแอดมิน)
เปิด Terminal ในโฟลเดอร์โปรเจกต์ แล้วพิมพ์คำสั่ง:
```bash
node sync_trainees_to_google.js
```
ระบบจะแสดงสถานะการอัปโหลดของแต่ละคน และสรุปผลเมื่อเสร็จสิ้น

---

## 🔄 การทำงานหลังจากซิงค์เสร็จแล้ว
* หน้าเว็บจะแสดงป้ายสถานะสีเขียว **`☁️ Google Sheets & Drive`** ที่แถบด้านบน
* เมื่อมีการ **แก้ไขชื่อ/ตำแหน่ง/อัปโหลดรูปใหม่** หรือ **ลบคน** จากหน้าเว็บ ข้อมูลจะถูกบันทึกส่งไปยัง Google Sheets และ Google Drive ให้ทันทีโดยไม่ต้องมากรอกเองในชีต
* หากเปิดหน้าเว็บตอนไม่มีอินเทอร์เน็ต ระบบจะสลับไปอ่านภาพจากโฟลเดอร์ในเครื่องอย่างปลอดภัย (Offline Fallback)
